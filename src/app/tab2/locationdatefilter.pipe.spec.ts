import { LocationdatefilterPipe } from './locationdatefilter.pipe';

describe('LocationdatefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationdatefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
